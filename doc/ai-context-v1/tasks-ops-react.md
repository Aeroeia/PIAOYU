# 运维场景任务清单（ReAct）

## 目标
- 在现有聊天入口内提供“查询诊断型 ReAct”能力，支持日志/指标排障闭环。
- 保持高危动作门禁策略：只给人工建议，不自动执行变更操作。

## 任务清单
- [x] 1. 定义 ReAct 输出协议：`Thought / Action / Observation / Conclusion / Next`
- [x] 2. 实现运维 ReAct 服务：`OpsReactService`
- [x] 3. 实现循环控制：最大 5 轮，满足终止条件提前结束
- [x] 4. 实现高危动作门禁：命中重启/回滚/扩缩容时返回人工确认提示
- [x] 5. 实现降级策略：工具/模型异常时返回人工排查建议
- [x] 6. 接入主编排器：`OPS` 场景进入 ReAct 服务
- [x] 7. 保持边界：本轮仅查询诊断，不新增自动执行型 MCP 工具
- [ ] 8. Maven 环境编译与故障演练回归（当前环境无 `mvn/mvnw`）

## 交付物
- `OpsReactService`（查询诊断型 ReAct）
- 主编排器 `OPS` 分支接线
- 运维任务文档边界说明（无自动高危执行）

## 每轮改动日志
### Round 1
- 目标: 落地运维 ReAct 查询诊断闭环并接入编排链路
- 代码改动:
  - 新增 `OpsReactService`（多轮循环、终止条件、降级策略）
  - 新增高危动作门禁判断（重启/回滚/扩缩容只提示人工确认）
  - 编排器接入 `OPS` 分支并复用会话状态回写
- 接口变更: 无（继续复用 `/program/chat`、`/program/chat/mcp` 等入口）
- 数据变更: 无
- 验证结果: 代码静态检查完成；当前环境无 Maven，未执行编译和自动化测试
- 风险与回滚点:
  - 风险：ReAct 轮次输出依赖模型结构化遵循度
  - 回滚：可将 `OPS` 场景临时回退到 `GENERAL` 主问答链路
- 下一轮: 在有 Maven 环境执行运维排障案例回归（日志+指标组合）
